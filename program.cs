namespace Program
{
    public class Software
    {
        static void Main(string[] args)
        {
            System.Console.WriteLine("Estudando git...");
            System.Console.WriteLine("Comandos mostrados no curso: git config --global user.name \"NAME\"; git config --global user.email \"E-MAIL\"; git status.");
            System.Console.WriteLine("\tgit add; git commit; git restore.");
            System.Console.WriteLine("\tgit branch; git restore, git checkout \"nameBranch\"; git reset");
            System.Console.WriteLine("\tgit checkout -b \"nameBranch\"; git branch -d \"nameBranch\"");
            System.Console.WriteLine("\tgit pull; git clone \"gitURL\"");
            System.Console.WriteLine("\tgit stash; git stash list; git stash apply \"ID\", git stash show -p \"ID\"");
        }
    }
}